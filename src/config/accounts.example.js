/**
 * Configuración estándar para múltiples cuentas de WhatsApp.
 * Simplemente agrega el ID del webhook y su token correspondiente.
 */
module.exports = {
    // Webhook ID: Token de la API de WhatsApp
    "id": "token...",
};
